from modules.parse import GouvParser

if __name__ == "__main__":
    GouvParser()

    # infos = dict(
    #     date=now(),
    #     name=SRES.name,
    #     node=SRES.node,
    #     planning=1234,
    #     res=True,
    #     date_last_success=now(),
    #     reason="Guichet no5 (Haute-Garonne) -- Checked but nope",
    # )
    # make_notif(infos)
